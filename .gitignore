node_modules/
dist/
release/
# The bundled server payload is produced by scripts/bundle-server.mjs from a
# checkout of the server repo; it is a build artifact, never committed.
vendor/server/
# Built by mobile/scripts/build-www.mjs from the shared renderer sources.
mobile/www/
*.log
.DS_Store
# Android world-hosting build artifacts: the server payload zip and the
# Node runtime are staged by mobile/scripts/bundle-android-payload.mjs.
mobile/android/app/src/main/assets/server-payload.zip
mobile/android/app/src/main/assets/server-payload.json
mobile/android/app/src/main/jniLibs/
mobile/runtime/
mobile/.payload-*
